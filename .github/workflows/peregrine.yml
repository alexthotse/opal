name: Peregrine CI/CD

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:

jobs:
  lint-and-test:
    name: Lint & Test
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install ALSA headers
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libasound2-dev

      - name: Setup Bazel
        uses: bazel-contrib/setup-bazel@0.8.5
        with:
          bazelisk-cache: true

      - name: Setup Task
        uses: arduino/setup-task@v2
        with:
          version: 3.x
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Run Unit Tests
        run: task test

  build:
    name: Build Artifacts
    needs: lint-and-test
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install ALSA headers
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libasound2-dev

      - name: Setup Bazel
        uses: bazel-contrib/setup-bazel@0.8.5
        with:
          bazelisk-cache: true

      - name: Setup Task
        uses: arduino/setup-task@v2
        with:
          version: 3.x
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Nix
        if: runner.os == 'Linux'
        uses: DeterminateSystems/nix-installer-action@v16

      - name: Enable Nix Cache
        if: runner.os == 'Linux'
        uses: DeterminateSystems/magic-nix-cache-action@v8

      - name: Build Falcon Backend (Gleam)
        run: task build:falcon

      - name: Build Peregrine Frontend (Go)
        run: task build:peregrine

      - name: Verify Binary Exists
        run: |
          if [ ! -f ./bazel-bin/peregrine/peregrine_/peregrine ]; then
            echo "Bazel build failed to produce the executable"
            exit 1
          fi
          ./bazel-bin/peregrine/peregrine_/peregrine -h || echo "Binary exists and is executable"

      - name: Install Nix
        if: runner.os == 'Linux'
        uses: DeterminateSystems/nix-installer-action@main
      
      - name: Build OCI Container (Alpine Linux Base)
        # OCI builds natively on Linux. Avoid macOS Nix cross-compilation quirks for Docker outputs.
        if: runner.os == 'Linux'
        run: task build:container

  release:
    name: Publish Release & Container
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install ALSA headers
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libasound2-dev

      - name: Setup Bazel
        uses: bazel-contrib/setup-bazel@0.8.5
        with:
          bazelisk-cache: true

      - name: Setup Task
        uses: arduino/setup-task@v2
        with:
          version: 3.x
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v16

      - name: Enable Nix Cache
        uses: DeterminateSystems/magic-nix-cache-action@v8

      - name: Build Native Linux Binary
        run: task build:peregrine

      - name: Tarball Artifact
        run: |
          tar -czvf peregrine-${{ github.ref_name }}-linux-amd64.tar.gz -C bazel-bin/peregrine/peregrine_ peregrine

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: peregrine-${{ github.ref_name }}-linux-amd64.tar.gz
          generate_release_notes: true

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@main

      - name: Build OCI Container Image
        run: task build:container

      - name: Log in to GitHub Container Registry (GHCR)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish to GHCR
        run: |
          # Load the Nix-built OCI container into docker daemon
          docker load < result
          
          IMAGE_NAME="ghcr.io/${{ github.repository }}"
          
          # The nix dockerTools defaults the tag to 'latest'. We will retag it with the explicit version.
          docker tag ghcr.io/alexthotse/peregrine:latest $IMAGE_NAME:${{ github.ref_name }}
          docker tag ghcr.io/alexthotse/peregrine:latest $IMAGE_NAME:latest
          
          # Push both the version tag and the latest tag
          docker push $IMAGE_NAME:${{ github.ref_name }}
          docker push $IMAGE_NAME:latest
          echo "GHCR pushing completed successfully"
