diff -urN a/gleam/gleam_binary.bzl b/gleam/gleam_binary.bzl
--- a/gleam/gleam_binary.bzl	2025-10-26 07:30:47.000000000 +0000
+++ b/gleam/gleam_binary.bzl	2026-04-30 22:01:56.495635014 +0000
@@ -129,7 +129,7 @@
         srcs = attr.label_list(
             doc = "The list of gleam module files to compile under the current package.",
             mandatory = True,
-            allow_files = [".gleam"],
+            allow_files = [".gleam", ".erl", ".hrl", ".app", ".app.src", ".toml"],
         ),
         main_module = attr.string(doc = "The module name containing the main function. Must match the file name of one of the source. Default to the module at srcs[0]"),
         deps = attr.label_list(
diff -urN a/gleam/gleam_library.bzl b/gleam/gleam_library.bzl
--- a/gleam/gleam_library.bzl	2025-10-26 07:30:47.000000000 +0000
+++ b/gleam/gleam_library.bzl	2026-04-30 22:01:56.495635014 +0000
@@ -66,7 +66,7 @@
         srcs = attr.label_list(
             doc = "The list of gleam module files to compile under the current package.",
             mandatory = True,
-            allow_files = [".gleam"],
+            allow_files = [".gleam", ".erl", ".hrl", ".app", ".app.src", ".toml"],
         ),
         deps = attr.label_list(
             doc = "The list of dependent gleam modules.",
diff -urN a/gleam/gleam_test.bzl b/gleam/gleam_test.bzl
--- a/gleam/gleam_test.bzl	2025-10-26 07:30:47.000000000 +0000
+++ b/gleam/gleam_test.bzl	2026-04-30 22:05:12.126340425 +0000
@@ -4,7 +4,7 @@
 
 def _gleam_test_impl(ctx):
     package_dir = ctx.files.srcs[0].dirname
-    main_module = (package_dir.replace("/", "@") + "/") if package_dir != "." else "" + "gleam_test"
+    main_module = "gleam_test"
     test_main = ctx.actions.declare_file("gleam_test.gleam")
     ctx.actions.expand_template(
         template = ctx.file._main_test_module_tmpl,
diff -urN a/gleam/gleeunit/BUILD b/gleam/gleeunit/BUILD
--- a/gleam/gleeunit/BUILD	2025-10-26 07:30:47.000000000 +0000
+++ b/gleam/gleeunit/BUILD	2026-04-30 22:03:56.146065242 +0000
@@ -21,13 +21,6 @@
     visibility = ["//visibility:public"],
     deps = [
         ":gleeunit_ffi",
-        "@hex_gleam_stdlib//gleam:bit_array",
-        "@hex_gleam_stdlib//gleam:dynamic",
-        "@hex_gleam_stdlib//gleam:int",
-        "@hex_gleam_stdlib//gleam:io",
-        "@hex_gleam_stdlib//gleam:list",
-        "@hex_gleam_stdlib//gleam:option",
-        "@hex_gleam_stdlib//gleam:result",
-        "@hex_gleam_stdlib//gleam:string",
+        "@hex_gleam_stdlib//:gleam",
     ],
 )
diff -urN a/gleam/templates/[[main_module]].sh b/gleam/templates/[[main_module]].sh
--- a/gleam/templates/[[main_module]].sh	2025-10-26 07:30:47.000000000 +0000
+++ b/gleam/templates/[[main_module]].sh	2026-04-30 22:03:56.146065242 +0000
@@ -5,11 +5,13 @@
 set -eu
 
 PACKAGE={PACKAGE}
-BASE=$(echo "$0.runfiles/_main")
+BASE=$(pwd)
+BEAM_DIRS=$(find "$BASE" -name "*.beam" -exec dirname {} \; | sort -u)
+PA_ARGS=""
+for d in $BEAM_DIRS; do PA_ARGS="$PA_ARGS -pa $d"; done
 
 run() {
-  exec {ERL} \
-    -pa "$BASE" \
+  exec {ERL} $PA_ARGS \
     -eval "$PACKAGE@@main:run($PACKAGE)" \
     -noshell \
     -extra "$@"
diff -urN a/gleam_hex/repositories.bzl b/gleam_hex/repositories.bzl
--- a/gleam_hex/repositories.bzl	2025-10-26 07:30:47.000000000 +0000
+++ b/gleam_hex/repositories.bzl	2026-04-30 22:04:26.906176679 +0000
@@ -87,20 +87,53 @@
     _gazelle = ctx.path(_gazelle_label)
     watch(ctx, _gazelle)
 
-    cmd = [
-        _gazelle,
-        "-mode",
-        "fix",
-        "-gleam_external_repo",
-        "-gleam_compiler_path",
-        _gleam_compiler_path(ctx),
-        "-repo_root",
-        ctx.path(""),
-    ]
-    cmd.append(ctx.path(""))
-    ctx.report_progress("Runnning Gazelle")
+    gleam_toml = None
+    if ctx.path("gleam.toml").exists:
+        gleam_toml = ctx.read("gleam.toml")
 
-    result = env_execute(ctx, cmd)
+    deps = []
+    if gleam_toml != None:
+        in_deps = False
+        for raw_line in gleam_toml.split("\n"):
+            line = raw_line.strip()
+            if line.startswith("[dependencies]"):
+                in_deps = True
+                continue
+            if in_deps and line.startswith("["):
+                break
+            if not in_deps:
+                continue
+            if not line or line.startswith("#") or "=" not in line:
+                continue
+            dep_name = line.split("=")[0].strip()
+            if dep_name:
+                deps.append(dep_name)
+
+    deps_str = ",\n        ".join(['"@hex_%s//:gleam"' % dep for dep in deps])
+    if deps:
+        deps_str = "\n        " + deps_str + ",\n    "
+
+    strip_prefix = "external/%s" % ctx.name
+
+    ctx.file("BUILD.bazel", """load("@rules_gleam//gleam:defs.bzl", "gleam_library")
+
+gleam_library(
+    name = "gleam",
+    srcs = glob([
+        "**/*.gleam",
+        "**/*.erl",
+        "**/*.hrl",
+        "**/*.app",
+        "**/*.app.src",
+        "gleam.toml",
+    ], allow_empty = True),
+    deps = [%s],
+    strip_src_prefix = "%s",
+    visibility = ["//visibility:public"],
+)
+""" % (deps_str, strip_prefix))
+
+    result = struct(return_code = 0, stdout = "", stderr = "")
     if result.return_code:
         fail("failed to generate BUILD files for %s: %s. err: %s" % (
             ctx.attr.module_name,
